import React, { useState, useEffect } from "react";
import styled from "styled-components";

const star = "⭐";

function LittleStars(props) {
  const { count } = props;
  const stars = [];
  :hover {
    background-color: #d0f0d0;
  }
  let i = 0;
  while (i < count) {}
  return <div>{star}</div>;
}

export default LittleStars;
>{star}</div>;
}

export default LittleStars;
